import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eu.ra1g.mytankranking',
  appName: 'MyTankRanking',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
